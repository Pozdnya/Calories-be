export enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user',
}

export enum AppLanguageEnum {
  UA = 'ua',
  EN = 'en',
}

export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}
